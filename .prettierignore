# Thư viện & sản phẩm build
node_modules
dist
coverage

# Lockfile do npm tự quản lý
package-lock.json

# Dữ liệu sinh tự động khi build (scripts/gen-data-manifest.mjs ...) — không format
public/data/

# Data blob lớn (nguồn dữ liệu, không phải code) — bỏ qua để diff format gọn
src/data/lessons.json
src/data/dictionary.backup.json

# File build/cache tạm
vite.config.ts.timestamp-*.mjs
*.tsbuildinfo

# Playwright artefacts sinh tự động
playwright-report/
test-results/
blob-report/

# Thư mục "drop-in" khung tham khảo (không phải mã nguồn dự án) — có .prettierrc
# riêng yêu cầu prettier-plugin-tailwindcss (không cài), khiến `prettier --check .`
# lỗi. Đã được eslint/tsc bỏ qua; bỏ qua ở prettier cho nhất quán.
_framework-dropins/

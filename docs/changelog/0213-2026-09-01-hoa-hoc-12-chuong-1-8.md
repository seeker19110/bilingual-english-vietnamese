# 0213 — 2026-09-01 — Hoá học 12 Chương 1-8 (30 bài, GĐ3 đợt 3)

Đặc tả: `docs/goals/2026-08-31-mon-hoc-toan-ly-hoa-sinh.md` (Goal M2/S2, hoàn thành). Đối chiếu mục lục SGK "Kết nối tri thức" Hoá 12 thật (ảnh scan tải từ `taphuan.nxbgd.vn`, OCR tại chỗ).

## Đã làm

1. **`packages/subject-chemistry` mở rộng**:
   - Thêm `lessons/hoa12c{1..8}.ts` chứa toàn bộ **30 bài học Hoá 12** chia làm 8 chương:
     - Chương 1: Ester - Lipid (3 bài, từ Bài 1 đến 3) — Khái niệm ester/lipid, xà phòng và chất giặt rửa, ôn tập.
     - Chương 2: Carbohydrate (4 bài, từ Bài 4 đến 7) — Glucose và fructose, saccharose và maltose, tinh bột và cellulose, ôn tập.
     - Chương 3: Hợp chất chứa nitrogen (4 bài, từ Bài 8 đến 11) — Amine, amino acid và peptide, protein và enzyme, ôn tập.
     - Chương 4: Polymer (3 bài, từ Bài 12 đến 14) — Đại cương polymer, vật liệu polymer, ôn tập.
     - Chương 5: Pin điện và điện phân (3 bài, từ Bài 15 đến 17) — Thế điện cực và nguồn điện hoá học, điện phân, ôn tập.
     - Chương 6: Đại cương về kim loại (6 bài, từ Bài 18 đến 23) — Cấu tạo và liên kết, tính chất vật lí và hoá học, tách kim loại, hợp kim, ăn mòn, ôn tập.
     - Chương 7: Nguyên tố nhóm IA và nhóm IIA (3 bài, từ Bài 24 đến 26) — Nguyên tố nhóm IA, nguyên tố nhóm IIA, ôn tập.
     - Chương 8: Sơ lược về dãy kim loại chuyển tiếp thứ nhất và phức chất (4 bài, từ Bài 27 đến 30) — Đại cương kim loại chuyển tiếp dãy thứ nhất, sơ lược phức chất, tính chất và ứng dụng của phức chất, ôn tập.
   - Mỗi bài học bám sát khung 5 bước tiêu chuẩn cao nhất: Móc thực tế (hook) → Lý thuyết cốt lõi (theory) → Ví dụ mẫu tự soạn kèm giải chi tiết từng bước (workedExample) → 2 câu hỏi kiểm tra tự chấm bằng engine (checkQuestions) → 2-4 thẻ nhớ ôn tập (srsCards).
   - Tích hợp toàn bộ bài học vào registry `CHEM_LESSONS` trong `lessons.ts` (tổng số lên 72 bài học gồm Hoá 10, 11 và 12).
   - Đánh dấu thuộc tính `reviewStatus: 'draft'` cho toàn bộ 30 bài học mới vì nội dung được tổng hợp từ bản thảo chưa qua giáo viên thật duyệt.

2. **Chất lượng nội dung & Tự động chấm**:
   - Mọi câu hỏi kiểm tra tự chấm đều được kiểm chứng thành công bằng engine chấm thực `core-grading` (qua hàm `gradeAnswer` không gọi AI, bảo đảm tính tất định và chấm đúng).
   - Kiểm tra `npx vitest` xác nhận 100% bài học vượt qua kiểm tra Zod Schema `ChemLessonSchema` mà không bị lỗi cấu trúc dữ liệu.

## Luật số 1 & bất biến giữ nguyên

- Không sử dụng bất kỳ cơ chế AI nào trong luồng chấm bài.
- Nội dung lý thuyết và ví dụ mẫu tự biên soạn 100% để tránh vấn đề bản quyền SGK.
- Các bài học mới giữ nguyên nhãn `reviewStatus: 'draft'`.

## Cố ý KHÔNG làm

- Chưa nối bài học Hoá 12 vào UI giao diện người dùng. Sẽ thực hiện nối một loạt sau khi có đủ bài của các môn tiếp theo (Vật lí, Sinh học, v.v.).
- Chưa tạo migration cơ sở dữ liệu. Bài học vẫn được biên dịch trực tiếp dạng hằng số trong package.

## Bằng chứng

- `npx vitest run packages/subject-chemistry` ✅ 6/6 test suite pass (bao gồm 72 bài học Hoá 10, 11 và 12).
- `npx tsc -b packages/subject-chemistry` ✅ compilation sạch 0 lỗi.
- `npx eslint packages/subject-chemistry/lessons/hoa12c*.ts` ✅ 0 lỗi linting.
- `npx prettier --check packages/subject-chemistry/lessons/hoa12c*.ts` ✅ 100% đúng style định dạng.

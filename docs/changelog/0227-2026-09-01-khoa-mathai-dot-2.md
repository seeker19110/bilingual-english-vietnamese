# 0227 — Khoá `mathai` "Toán Thiết Yếu cho AI" — đợt 2/5 cụm "Kỹ sư AI thực chiến"

**Ngày:** 2026-09-01 · **Nhánh:** `claude/bat-tay-soan-dot-2-mathai` (rẽ từ nhánh đợt 1
`claude/bat-tay-soan-dot-1-a9j9e2` — kế thừa sẵn phần nới regex 6 tiền tố, xem đợt 0226)
**Đặc tả:** `docs/specs/2026-09-01-cum-6-khoa-ai-engineer.md` (khung) +
`docs/specs/2026-09-01-cum-6-khoa-khung-thi-hanh.md` (hạ tầng, mục 4.2) +
`docs/specs/2026-09-01-mathai-bai-hoc-chi-tiet.md` (nội dung 13 bài)
**Tiếp nối:** đợt 0226 (`pyai`, đợt 1/5) — quy trình thi hành giống hệt, không cần nới lại regex.

## Việc đã làm

Đợt 2/5 của cụm 6 khoá ngắn "Kỹ sư AI thực chiến". Thêm khoá `mathai` "Toán Thiết Yếu cho AI"
— 13 bài, 3 chương, `prerequisites: ['Khoá Python / AI Cơ Bản (pyai) hoặc biết Python căn bản']`:

- `packages/subject-programming/courses/mathai.ts` — khoá mới, 3 chương (`mathai-c1..c3`):
  Xác suất & thống kê (5 bài) · Đại số tuyến tính (4 bài) · Giải tích & tối ưu hoá (4 bài).
- `packages/subject-programming/lessons/mathaiu1..3.ts` — 13 bài học 8 bước, copy nguyên văn
  từ đặc tả (mỗi bài một object trong file đặc tả, ghép thành mảng khi thi hành).
- Nới `ShortCourseId` (`courses/types.ts`) + đăng ký `courses/registry.ts` + `lessons.ts`.
- Thêm `mathai-u` vào danh sách tiền tố "unit ảo" ở `lessons.test.ts` — KHÔNG cần nới lại regex
  `lessonId`/`unitId` ở `lessonTypes.ts`/`progress.ts`/`feedback.ts` vì đợt 1 đã nới đủ 6 tiền
  tố một lần (đúng kế hoạch ở đặc tả khung mục 3).

## Khác biệt so với đợt 1

Không phát hiện lỗi nội dung nào khi chạy cổng python3 thật (đợt 1 phát hiện 5 lỗi ở `pyai` —
xem đợt 0226). Đặc tả `mathai` viết mỗi bài `input()`+`print()` tuần tự không xen lồng vòng
lặp, và mọi `predict.choices` đã dùng đúng giá trị output thật (không mô tả bằng lời) — nên
không có ca `expected` nhiều dòng bị echo `input()` chen giữa, không có ca lựa chọn sai vô
tình khớp output, không có thẻ SRS trùng câu hỏi.

## Bằng chứng kiểm chứng

- `npm run typecheck` · `npm run lint` (0 cảnh báo) · `npm run format:check` · `npm run build`
  → xanh.
- `npx vitest run packages/subject-programming` → **49/49 file, 3393/3393 test xanh** (tăng
  đúng +91 so với đợt 1: 39 test python3 cho 13 bài mới + 52 thẻ SRS mới).
- `npm test` (toàn repo) → **532/532 file, 10283/10283 test xanh**.
- `npm run audit:lessons` → **0 lỗi** (307 bài · 7 khoá ngắn, tăng đúng +13 bài/+1 khoá).

## Việc tiếp theo

Đợt 3 — khoá `mlds` "Machine Learning & Data Science" (7 bài mới + tham chiếu 10 bài `ml` có
sẵn), theo đặc tả sẽ soạn tiếp ở `docs/specs/2026-09-01-mlds-bai-hoc-chi-tiet.md`.

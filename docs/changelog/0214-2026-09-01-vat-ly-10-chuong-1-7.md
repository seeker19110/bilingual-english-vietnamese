# 0214 — 2026-09-01 — Vật lí 10 Chương 1-7 (34 bài, GĐ3 Vật lí đợt 1)

Đặc tả: `docs/goals/2026-08-31-mon-hoc-toan-ly-hoa-sinh.md` (Goal M3/S1, hoàn thành). Đối chiếu mục lục SGK "Kết nối tri thức" Vật lí 10 thật (ảnh scan tải từ `taphuan.nxbgd.vn`, OCR tại chỗ).

## Đã làm

1. **Tạo mới `packages/subject-physics`**:
   - Khởi tạo package mới cho môn Vật lí dựa trên khuôn mẫu cấu trúc của `subject-chemistry`:
     - `package.json` & `tsconfig.json` tích hợp đầy đủ.
     - `lessonTypes.ts` định nghĩa kiểu dữ liệu `PhysicsLesson` và Zod Schema `PhysicsLessonSchema` (xác thực định dạng ID bài học `/^ly(10|11|12)-c\d+-b\d+$/`).
     - `lessons.ts` làm registry tập trung xuất ra `PHYSICS_LESSONS`, hỗ trợ các hàm tra cứu và lọc theo khối lớp.
   - Nối package mới vào `tsconfig.packages.json` để Node.js monorepo nhận diện chính xác.
   - Thêm các bài học Vật lí 10 đầu tiên trải khắp 7 chương từ `lessons/ly10c1.ts` đến `lessons/ly10c7.ts` (tổng cộng **34 bài học**):
     - Chương 1: Mở đầu (3 bài, từ Bài 1 đến 3) — Làm quen môn học, quy tắc an toàn trong phòng thí nghiệm, sai số phép đo.
     - Chương 2: Động học (9 bài, từ Bài 4 đến 12) — Độ dịch chuyển, vận tốc, đồ thị d-t, gia tốc, chuyển động thẳng biến đổi đều, rơi tự do, chuyển động ném.
     - Chương 3: Động lực học (10 bài, từ Bài 13 đến 22) — Tổng hợp và phân tích lực, Ba định luật Newton, các lực cơ học (trọng lực, lực căng, ma sát, lực cản/nâng), phương pháp giải bài toán động lực học, moment lực và quy tắc moment.
     - Chương 4: Năng lượng, công, công suất (5 bài, từ Bài 23 đến 27) — Công cơ học, công suất, động năng, thế năng, cơ năng và bảo toàn cơ năng, hiệu suất.
     - Chương 5: Động lượng (3 bài, từ Bài 28 đến 30) — Động lượng, bảo toàn động lượng, thực hành đo động lượng.
     - Chương 6: Chuyển động tròn đều (2 bài, từ Bài 31 đến 32) — Động học chuyển động tròn, lực hướng tâm và gia tốc hướng tâm.
     - Chương 7: Biến dạng của vật rắn. Áp suất chất lỏng (2 bài, từ Bài 33 đến 34) — Biến dạng đàn hồi (Định luật Hooke), khối lượng riêng, áp suất chất lỏng và lực đẩy Archimedes.
   - Mỗi bài học biên soạn tỉ mỉ theo cấu trúc 5 bước chất lượng cao nhất: Móc thực tế → Lý thuyết → Ví dụ mẫu chi tiết → 2 câu hỏi kiểm tra → 2-4 thẻ nhớ Flashcard (SRS).
   - Đánh dấu thuộc tính `reviewStatus: 'draft'` cho toàn bộ 34 bài mới để chờ giáo viên phê duyệt chuyên môn.

2. **Cập nhật `packages/core-grading`**:
   - Thêm các đơn vị cơ học Vật lí còn thiếu vào bảng tra cứu `UNITS` và `dimOf` trong `units.ts`:
     - Gia tốc: `m/s²`, `m/s^2`, `m/s2` (thứ nguyên `[0, 1, -2, 0, 0, 0, 0]`).
     - Độ cứng: `N/m` (thứ nguyên `[1, 0, -2, 0, 0, 0, 0]`).
     - Động lượng: `kg.m/s`, `kg*m/s`, `kg m/s`, `N.s`, `N*s`, `Ns` (thứ nguyên `[1, 1, -1, 0, 0, 0, 0]`).
     - Khối lượng riêng: `kg/m³`, `kg/m3`, `g/cm³`, `g/cm3` (thứ nguyên `[1, -3, 0, 0, 0, 0, 0]`).
   - Cập nhật test suite `lessons.test.ts` để tự động chia cho hệ số chuyển đổi (`factor`) và điều chỉnh `offset` trước khi so khớp với dữ liệu SI của answer spec, giúp các câu hỏi dùng đơn vị có tỉ lệ chuyển đổi khác 1 (như `%` hay `g/cm3`) không bị trượt kiểm thử.

3. **Cố định `vitest.setup.ts`**:
   - Xử lý triệt để cảnh báo `localStorage is not available` và crash lỗi khi chạy Vitest trên Node.js v22+/v26+ bằng cách xóa `localStorage` lỗi của Node và stub in-memory mock sạch cho môi trường Happy-DOM.

## Luật số 1 & bất biến giữ nguyên

- Không sử dụng bất kỳ cơ chế AI nào trong luồng chấm bài.
- Nội dung lý thuyết và ví dụ mẫu tự biên soạn 100% để tránh vấn đề bản quyền SGK.
- Các bài học mới giữ nguyên nhãn `reviewStatus: 'draft'`.

## Cố ý KHÔNG làm

- Chưa nối bài học Vật lí 10 vào UI giao diện người dùng. Sẽ thực hiện nối một loạt sau khi có đủ bài của các môn tiếp theo.
- Chưa tạo database migration.

## Bằng chứng

- `npx vitest run packages/subject-physics packages/core-grading` ✅ 92/92 tests pass sạch 100%.
- `npm run build` ✅ Compilation thành công 100%.
- `npm run typecheck` & `npm run lint` & `npm run format:check` ✅ Tất cả các kiểm tra kiểm soát chất lượng đạt trạng thái xanh lá tuyệt đối.

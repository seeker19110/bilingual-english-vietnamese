# 0212 — 2026-08-31 — Hoá học 11 Chương 1-6 (25 bài, GĐ3 đợt 2)

Đặc tả: `docs/goals/2026-08-31-mon-hoc-toan-ly-hoa-sinh.md` (Goal M2/S1, hoàn thành). Đối chiếu mục lục SGK "Kết nối tri thức" Hoá 11 thật (ảnh scan tải từ `taphuan.nxbgd.vn`, OCR tại chỗ).

## Đã làm

1. **`packages/subject-chemistry` mở rộng**:
   - Thêm `lessons/hoa11c{1..6}.ts` chứa toàn bộ **25 bài học Hoá 11** chia làm 6 chương:
     - Chương 1: Cân bằng hoá học (3 bài, từ Bài 1 đến 3) — Khái niệm cân bằng, cân bằng trong dung dịch, ôn tập.
     - Chương 2: Nitrogen - Sulfur (6 bài, từ Bài 4 đến 9) — Nitrogen, ammonia, oxit nitơ và HNO₃, sulfur và SO₂, H₂SO₄ và muối sunfat, ôn tập.
     - Chương 3: Đại cương hoá học hữu cơ (5 bài, từ Bài 10 đến 14) — Nhập môn hữu cơ, phương pháp tách, công thức phân tử, cấu tạo hoá học, ôn tập.
     - Chương 4: Hydrocarbon (4 bài, từ Bài 15 đến 18) — Alkane, hydrocarbon không no (alkene/alkyne), arene (thơm), ôn tập.
     - Chương 5: Dẫn xuất halogen - Alcohol - Phenol (4 bài, từ Bài 19 đến 22) — Dẫn xuất halogen, alcohol, phenol, ôn tập.
     - Chương 6: Hợp chất carbonyl - Carboxylic acid (3 bài, từ Bài 23 đến 25) — Hợp chất carbonyl (aldehyde/ketone), carboxylic acid, ôn tập.
   - Mỗi bài học bám sát khung 5 bước tiêu chuẩn cao nhất: Móc thực tế (hook) → Lý thuyết cốt lõi (theory) → Ví dụ mẫu tự soạn kèm giải chi tiết từng bước (workedExample) → 2 câu hỏi kiểm tra tự chấm bằng engine (checkQuestions) → 2-4 thẻ nhớ ôn tập (srsCards).
   - Tích hợp toàn bộ bài học vào registry `CHEM_LESSONS` trong `lessons.ts`.
   - Đánh dấu thuộc tính `reviewStatus: 'draft'` cho toàn bộ 25 bài học mới vì nội dung được tổng hợp từ kho kiến thức chưa qua giáo viên thật duyệt.

2. **Chất lượng nội dung & Tự động chấm**:
   - Mọi câu hỏi kiểm tra tự chấm đều được kiểm chứng thành công bằng engine chấm thực `core-grading` (qua hàm `gradeAnswer` không gọi AI, bảo đảm tính tất định và rộng lượng trong cú pháp).
   - Kiểm tra `npx vitest` xác nhận 100% bài học vượt qua kiểm tra Zod Schema `ChemLessonSchema` mà không bị lỗi cấu trúc dữ liệu.

## Luật số 1 & bất biến giữ nguyên

- Không sử dụng bất kỳ cơ chế AI nào trong luồng chấm bài.
- Nội dung lý thuyết và ví dụ mẫu tự biên soạn 100% để tránh vấn đề bản quyền SGK.
- Các bài học mới giữ nguyên nhãn `reviewStatus: 'draft'`.

## Cố ý KHÔNG làm

- Chưa nối bài học Hoá 11 vào UI giao diện người dùng. Sẽ thực hiện nối một loạt sau khi có đủ bài Hoá 12.
- Chưa tạo migration cơ sở dữ liệu. Bài học vẫn được biên dịch trực tiếp dạng hằng số trong package.

## Bằng chứng

- `npx vitest run packages/subject-chemistry` ✅ 6/6 test suite pass (bao gồm 42 bài học Hoá 10 và Hoá 11).
- `npx tsc -b packages/subject-chemistry` ✅ compilation sạch 0 lỗi.
- `npx eslint packages/subject-chemistry/lessons/hoa11c*.ts` ✅ 0 lỗi linting.
- `npx prettier --check packages/subject-chemistry/lessons/hoa11c*.ts` ✅ 100% đúng style định dạng.

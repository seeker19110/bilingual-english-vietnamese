# Lấy key tại: https://console.anthropic.com/
# Dùng Claude Haiku — rẻ nhất, đủ tốt cho MVP
#
# QUAN TRỌNG: không có tiền tố VITE_ — key này chỉ được đọc ở phía server
# (vite.config.ts lúc "npm run dev", api/claude.ts lúc deploy lên Vercel).
# Nếu thêm VITE_ trước biến này, key sẽ bị lộ ra cho mọi người dùng web thấy được.
ANTHROPIC_API_KEY=sk-ant-...

# Khi deploy lên Vercel: vào Project Settings → Environment Variables,
# thêm ANTHROPIC_API_KEY (KHÔNG đặt tiền tố VITE_) với giá trị key thật.

# ── Cache phát âm từ điển (api/pronunciation.ts) ───────────────────────────
# Hướng dẫn tạo từng biến: xem PRONUNCIATION_CACHE_SETUP.md
# Cũng KHÔNG có tiền tố VITE_ — chỉ api/pronunciation.ts (chạy ở server) đọc được.

# Lấy ở Supabase Dashboard → Project Settings → API → Project URL
SUPABASE_URL=https://xxxxx.supabase.co

# Lấy ở Supabase Dashboard → Project Settings → API → service_role key (bí mật!)
# Key này có toàn quyền với database, KHÔNG đưa vào code, KHÔNG commit lên Git.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...

# Lấy ở Google Cloud Console → APIs & Services → Credentials (sau khi bật Text-to-Speech API)
GOOGLE_TTS_API_KEY=AIza...
